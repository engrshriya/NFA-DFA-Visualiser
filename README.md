# NFA-DFA-Visualiser
This web application will demonstrate the subset construction method used to convert an NFA into an equivalent DFA. The user will input an NFA, and the application will generate the DFA step-by-step by forming state subsets and constructing the transition table.
